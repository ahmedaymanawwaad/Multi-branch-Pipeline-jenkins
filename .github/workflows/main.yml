name: first Workflow

on: 
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print wellcome
        run: echo "hello to my first pipline"

      - name: checkout repo
        uses: actions/checkout@v4

      - name: list repos
        run: ls -la
  
