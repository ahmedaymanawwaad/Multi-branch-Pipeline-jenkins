name: first Workflow

on: 
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print wellcome
        run: echo "hello to my first pipline"
  
